##
## Production Environment Variables Example
##
## Instructions:
##   1. Copy this file to .env in production
##   2. Fill in all values with your actual credentials
##   3. Never commit .env to version control
##   4. Keep this file secure with proper permissions: chmod 600 .env
##

# ============================================
# FRONTEND ENVIRONMENT VARIABLES (Vite)
# ============================================
# Set these in Cloudflare Pages → Settings → Environment Variables

# API URL (if backend exists)
VITE_API_URL=https://api.shipforge.dev

# Analytics (optional)
VITE_GA_ID=G-XXXXXXXXXX

# Feature flags (optional)
VITE_ENABLE_PAYMENTS=true


# ============================================
# BACKEND ENVIRONMENT VARIABLES (Node.js)
# ============================================
# Set these in .env file on your VPS

# Node environment
NODE_ENV=production

# Server configuration
PORT=5000
HOST=0.0.0.0

# CORS - Frontend URLs allowed to access API
ALLOWED_ORIGINS=https://shipforge.dev,https://www.shipforge.dev

# Database
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Redis (if using for caching/sessions)
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=generate-a-long-random-string-minimum-32-characters
JWT_EXPIRES_IN=7d

# Session secret (if using express-session)
SESSION_SECRET=another-long-random-string-different-from-jwt

# ============================================
# PAYMENT PROVIDERS
# ============================================

# PayPal
PAYPAL_MODE=live
PAYPAL_CLIENT_ID=your-paypal-live-client-id
PAYPAL_CLIENT_SECRET=your-paypal-live-client-secret
PAYPAL_PLAN_ID=your-paypal-subscription-plan-id
PAYPAL_WEBHOOK_ID=your-paypal-webhook-id
PAYPAL_API=https://api-m.paypal.com

# Stripe (alternative)
STRIPE_SECRET_KEY=sk_live_xxxxxxxxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx
STRIPE_PRICE_ID=price_xxxxxxxxxxxxx

# ============================================
# EMAIL SERVICE
# ============================================

# SendGrid
SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
SENDGRID_FROM_EMAIL=noreply@shipforge.dev

# Mailgun (alternative)
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=mg.shipforge.dev

# SMTP (alternative)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=noreply@shipforge.dev

# ============================================
# CLOUD STORAGE (Optional)
# ============================================

# AWS S3
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-bucket-name

# Cloudflare R2 (S3-compatible)
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key
R2_SECRET_ACCESS_KEY=your-secret-key
R2_BUCKET_NAME=your-bucket-name

# ============================================
# MONITORING & LOGGING
# ============================================

# Sentry (error tracking)
SENTRY_DSN=https://xxxxxxxxxxxxx@sentry.io/xxxxxxxxxxxxx

# LogDNA / LogTail
LOGDNA_KEY=your-logdna-ingestion-key

# ============================================
# RATE LIMITING
# ============================================

RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# SECURITY
# ============================================

# API Keys for internal services
INTERNAL_API_KEY=generate-a-random-api-key

# Webhook secrets
WEBHOOK_SECRET=generate-a-webhook-secret

# ============================================
# FEATURE FLAGS
# ============================================

ENABLE_REGISTRATION=true
ENABLE_PAYMENTS=true
ENABLE_EMAIL=true
MAINTENANCE_MODE=false

# ============================================
# ADMIN SETTINGS
# ============================================

ADMIN_EMAIL=admin@shipforge.dev
SUPPORT_EMAIL=support@shipforge.dev

##
## SECURITY NOTES:
##
## 1. Generate strong random secrets:
##    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
##
## 2. Use different secrets for JWT and sessions
##
## 3. Rotate secrets periodically
##
## 4. Use environment-specific values (never use dev values in prod)
##
## 5. Back up this file securely (encrypted)
##
## 6. Set proper file permissions:
##    chmod 600 .env
##    chown www-data:www-data .env
##
