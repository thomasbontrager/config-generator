# ShipForge server-side secrets
# These should be set in your VPS environment (e.g. /home/shipforge/.env)
# and injected at runtime. Never commit real values.

# Server deploy user (used in CI deploy script)
SERVER_SSH_KEY=...
SERVER_HOST=...
SERVER_USER=...
SERVER_SSH_PORT=22
